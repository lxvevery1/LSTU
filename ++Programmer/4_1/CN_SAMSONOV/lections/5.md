RIP ничего не знает про сети

Какой версии RIP

пинги могут работать, но сеть не будет работать так как надо
Для продвинутых: Вместо RIP делать EIGRP

Области в OSPF не однотипные, есть области которые подключаются к
BetBone. Если за областью нет маршрутизаторов.
Есть некоторые области, которые называются stub - тупиковая область
В чем смысл?
Для того чтобы сеть нормально функционировала достаточно иметь маршрут
внутрь и дефолт наружу

У OSPF есть функция суммаризации на граничных роутерах (граничат между
областями)

Одна команда на маршрутизаторе "default automation regenerate". Если вдруг
адрес в статик маршруте поменяем, то все остальные маршрутизаторы обновят
адрес этого маршрутизатора

На OSPF информация о маршрутах отправляется LSL - link state (оповещение
о маршрутах)

7 типов - в какие-то области рассылаются
Самый простой способ

Улучшали во всех сетевых протоколах - безопасность

И ABR и ASBR - Border Router -> ASBR разделяют на границе автономной системы
