@startuml

skin rose

class Пользователь {
    +Имя: string
    +Email: string
    +Роль: string

    +СоздатьАккаунт(): void
    +ИзменитьРоль(): void
}

class Администратор extends Пользователь {
    +УправлениеБазойДанных(): void
    +УправлениеПользователями(): void
    +ПодтвердитьЗапрос(): void
}

class Менеджер extends Пользователь {
    +СоздатьЗапрос(): void
    +ПросмотретьИнформацию(): void
}

class Набор {
    +НомерНабора: int
    +Наименование: string
    +СтатусНабора: string
    +МестоХранения: string
    +Клиника: string
    +ОтветственныйМенеджер: string
    +ДатаПеремещения: date
    +КоличествоДнейВКлинике: int
    +Комментарий: string
    +Подразделение: string
    +Фото: string
    +Акт: string
    +ЖурналПеремещений: List<string>

    +ДобавитьФото(фото: string): void
    +РедактироватьФото(фото: string): void
    +ДобавитьАкт(акт: string): void
    +ДобавитьПеремещение(место: string, дата: date): void
}

class Клиника {
    +Название: string
    +Адрес: string
    +Телефон: string
    +Email: string

    +ДобавитьКлинику(): void
    +РедактироватьКлинику(): void
}

class Менеджер {
    +Имя: string
    +Фамилия: string
    +Email: string
    +Телефон: string

    +ДобавитьМенеджера(): void
    +РедактироватьМенеджера(): void
}

class ЖурналПеремещений {
    +Место: string
    +Дата: date

    +ДобавитьПеремещение(): void
}

class Запрос {
    +Статус: string

    +СоздатьЗапрос(): void
    +ПодтвердитьЗапрос(): void
}

class БазаДанных {
    +Папки: List<string>

    +СоздатьПапку(): void
    +УдалитьПапку(): void
    +СоздатьОтчет(): void
}

Набор "1" <-- "*" ЖурналПеремещений : имеет
Набор "1" --> "1" Клиника : принадлежит
Набор "many" <-- "1" Менеджер : ответственный за
Менеджер "1" --> "many" Запрос : создает
БазаДанных "1" --> "many" Набор : содержит
БазаДанных "1" --> "many" Клиника : содержит
БазаДанных "1" --> "many" Менеджер : содержит

Запрос "1" --* "1" Набор : содержит
Запрос "1" --* "1" Менеджер : содержит

@enduml
