@startuml
actor Менеджер
actor Администратор

    Менеджер --> (Просмотр доступных наборов) : просматривает
    (Просмотр доступных наборов) --> (Создание заказа) : инициирует

    Администратор --> (Просмотр списка заказов) : просматривает
    Администратор --> (Просмотр полного списка наборов) : просматривает
    (Просмотр полного списка наборов) --> (Редактирование полного списка наборов) : редатирует
    (Редактирование полного списка наборов) --> (Обновление данных БД) : Обновление списка
    (Просмотр списка заказов) --> (Формирование отчета) : формирует
    (Просмотр списка заказов) --> (Принятие заказа) : принимает
    (Принятие заказа) --> (Обновление данных БД) : обновляет статус заказа
@enduml
